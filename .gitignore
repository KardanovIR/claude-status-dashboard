node_modules/
dist/
.env
.env.local
*.log
.DS_Store

# APNs auth keys — never commit
keys/
*.p8

# IDE / editor
.idea/
.vscode/

# Local Claude Code settings (personal permissions, machine paths)
.claude/settings.local.json

# Personal scratch scripts — never commit
copy_*.sh
test.sh
content.html
config.merge.json

# Android local SDK path (machine-specific)
android/local.properties
android/.gradle/
android/build/
android/app/build/
android/keystore.properties
distribution/google-play/release/*.aab
